# Saily compatibility prototype

An interactive web prototype for a product-case exercise. It presents two eSIM compatibility flows:

1. **Compatibility reminder** — a low-effort reminder before checkout.
2. **Verify target device** — the recommended flow that checks the device that will use the eSIM, including purchases made on another device.

Open `index.html` in a browser to use the prototype.
